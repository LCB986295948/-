<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="T_Role_MenuDaoMapper">
	<sql id="t_role_menutable">
		T_ROLE_MENU
	</sql>
	<sql id="t_role_menukey">
		RMID,
		ROLEID,
		MENUID
	</sql>
	<sql id="t_role_menuvalue">
		#{rmid},
		#{roleid},
		#{menuid}
	</sql>
	<!-- 查询全部 -->
	<select id="query" parameterType="com.team.t_role_menu.entity.T_Role_Menu"
		resultType="map">
		select <![CDATA[Rownum rown]]>,
		<include refid="t_role_menukey"></include>
		from
		<include refid="t_role_menutable"></include>
	</select>
	<!-- 条件查询 -->
	<select id="queryBy" parameterType="com.team.t_role_menu.entity.T_Role_Menu"
		resultType="map">
		select <![CDATA[ROWNUM rown]]>,
		<include refid="t_role_menukey"></include>
		from
		<include refid="t_role_menutable"></include>
		<trim prefix="WHERE" prefixOverrides="AND|OR">
			<if test="t_role_menu.rmid!=null">
				RMID=#{t_role_menu.rmid,jdbcType=VARCHAR}
			</if>
			<if test="t_role_menu.roleid!=null">
				ROLE=#{t_role_menu.roleid,jdbcType=VARCHAR}
			</if>
			<if test="t_role_menu.menuid!=null">
				MENUID=#{t_role_menu.menuid,jdbcType=VARCHAR}
			</if>
		</trim>
	</select>
	<!-- 添加 -->
	<insert id="insert" parameterType="com.team.t_role_menu.entity.T_Role_Menu">
		<selectKey resultType="java.lang.String" order="BEFORE"
			keyProperty="rmid">
			select sys_guid() from dual
		</selectKey>
		insert into
		<include refid="t_role_menutable"></include>
		values(#{rmid,jdbcType=VARCHAR},#{roleid,jdbcType=VARCHAR},#{menuid,jdbcType=VARCHAR})
	
	</insert>
	<!-- 修改 -->
	<update id="update" parameterType="com.team.t_role_menu.entity.T_Role_Menu">
		update
		<include refid="t_role_menutable"></include>
		<trim prefix="set" prefixOverrides=",">
			<if test="roleid!=null">
				ROLEID=#{roleid,jdbcType=VARCHAR},
			</if>
			<if test="menuid!=null">
				MENUID=#{menuid,jdbcType=VARCHAR}
			</if>
		</trim>
		<where>
			RMID=#{rmid,jdbcType=VARCHAR}
		</where>
	</update>
	<!-- 删除 -->
	<delete id="delete" parameterType="com.team.t_role_menu.entity.T_Role_Menu">
		delete from
		<include refid="t_role_menutable"></include>
		<where>
			<if test="rmid!=null">
				RMID=#{rmid,jdbcType=VARCHAR}
			</if>
		</where>
	</delete>
</mapper>